import 'package:flutter/material.dart';

class RecuperarPass extends StatelessWidget {
const RecuperarPass({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}